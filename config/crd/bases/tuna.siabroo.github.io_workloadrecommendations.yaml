---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: workloadrecommendations.tuna.siabroo.github.io
spec:
  group: tuna.siabroo.github.io
  names:
    kind: WorkloadRecommendation
    listKind: WorkloadRecommendationList
    plural: workloadrecommendations
    shortNames:
    - wr
    - workreco
    singular: workloadrecommendation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.workload.type
      name: Workload
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.recommendations[*]
      name: Recs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadRecommendation is the Schema for the workloadrecommendations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              WorkloadRecommendationSpec defines the desired state — auto-populated
              by DiscoveryController from the source Deployment. Users have no
              reason to edit Spec; it is effectively read-only after creation.
            properties:
              targetRef:
                description: |-
                  TargetRef references the Deployment this recommendation is for.
                  Set by DiscoveryController, immutable after creation.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - targetRef
            type: object
          status:
            description: WorkloadRecommendationStatus holds the analyzer output.
            properties:
              conditions:
                description: |-
                  Conditions reflect the current state of analysis.
                  Standard k8s pattern; well-known types: Ready, MetricsAvailable,
                  WorkloadDetected, DataSufficient.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              current:
                description: Current settings read from the target Deployment.
                properties:
                  env:
                    additionalProperties:
                      type: string
                    description: |-
                      Env values for relevant runtime variables (GOMAXPROCS, GOMEMLIMIT, GOGC).
                      Map keys are env var names; values are the literal string set on the
                      container (empty string means unset).
                    type: object
                  resources:
                    description: Resources from the target container's spec.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              lastAnalyzedAt:
                description: LastAnalyzedAt is the time of the most recent successful
                  analysis.
                format: date-time
                type: string
              observed:
                description: Observed metrics from Prometheus.
                properties:
                  cpu:
                    description: CPU usage percentiles in millicores.
                    properties:
                      p50Millicores:
                        format: int64
                        type: integer
                      p95Millicores:
                        format: int64
                        type: integer
                      p99Millicores:
                        format: int64
                        type: integer
                    required:
                    - p50Millicores
                    - p95Millicores
                    - p99Millicores
                    type: object
                  gc:
                    description: GC pause percentiles in milliseconds.
                    properties:
                      pauseP99Ms:
                        type: number
                      pauseP99Window:
                        type: string
                    required:
                    - pauseP99Ms
                    - pauseP99Window
                    type: object
                  memory:
                    description: Memory working-set percentiles in bytes.
                    properties:
                      workingSetP95Bytes:
                        format: int64
                        type: integer
                      workingSetP99Bytes:
                        format: int64
                        type: integer
                    required:
                    - workingSetP95Bytes
                    - workingSetP99Bytes
                    type: object
                  window:
                    description: Window is the duration covered by the metrics (e.g.,
                      "24h").
                    type: string
                required:
                - window
                type: object
              observedGeneration:
                description: ObservedGeneration reflects the spec.targetRef generation
                  last analyzed.
                format: int64
                type: integer
              recommendations:
                description: Recommendations produced by the analyzer.
                items:
                  description: Recommendation is one suggested change to the target
                    Deployment.
                  properties:
                    current:
                      description: Current is the present value (or "(unset)").
                      type: string
                    field:
                      description: |-
                        Field is the path the recommendation applies to,
                        e.g. "resources.requests.cpu" or "env.GOMAXPROCS".
                      type: string
                    rationale:
                      description: Rationale is a human-readable explanation.
                      type: string
                    recommended:
                      description: Recommended is the suggested new value.
                      type: string
                    severity:
                      description: 'Severity is one of: info, warning, critical.'
                      enum:
                      - info
                      - warning
                      - critical
                      type: string
                  required:
                  - current
                  - field
                  - rationale
                  - recommended
                  - severity
                  type: object
                type: array
              workload:
                description: Workload describes what was detected.
                properties:
                  type:
                    description: 'Type is the workload family. Iter 1: "Go".'
                    type: string
                  version:
                    description: Version is the runtime version, if known (e.g., "1.26.0").
                    type: string
                required:
                - type
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
