apiVersion: v1
kind: Service
metadata:
  name: loadgen
  namespace: default
  labels:
    app: loadgen
spec:
  selector:
    app: loadgen
  ports:
    - name: http
      port: 8080
      targetPort: 8080
