apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: loadgen
  namespace: default
  labels:
    release: kube-prom
spec:
  selector:
    matchLabels:
      app: loadgen
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
