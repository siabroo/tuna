apiVersion: v1
kind: Service
metadata:
  name: sample-go-app
  namespace: default
  labels:
    app: sample-go-app
spec:
  selector:
    app: sample-go-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
